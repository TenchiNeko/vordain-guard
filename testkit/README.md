# Testkit

Shared test helpers and fixtures for future module tests.

Initial fixture areas:

- `fake-policy`: policy engine fixtures and fake policies
- `fake-vpn`: VPN engine and classifier fixtures
- `fake-relay`: encrypted relay client fakes
- `fixtures`: static data for classifier and event serialization tests

The current scaffold includes policy unit tests in `core/policy`.
