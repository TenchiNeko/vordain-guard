package com.vordain.guard.data.review

enum class SuggestedAllowScope {
    EXACT_DOMAIN,
    DOMAIN_AND_SUBDOMAINS,
    APP_ONLY_DOMAIN,
    TEMPORARY_EXACT_DOMAIN,
    TEMPORARY_DOMAIN_AND_SUBDOMAINS,
}
